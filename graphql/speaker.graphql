query Speaker(
  $slug: String
  $locale: SiteLocale
  $fallbackLocale: [SiteLocale!]
  $skip: IntType = "0"
) {
  speaker(filter: { slug: { eq: $slug } }, locale: $locale) {
    name
    picture {
      responsiveImage {
        srcSet
        webpSrcSet
        sizes
        src
        width
        height
        aspectRatio
        alt
        title
        bgColor
        base64
      }
    }
    title
    bio
    _allReferencingTalks(
      filter: { title: { isBlank: "false" } }
      orderBy: _createdAt_DESC
      first: "100"
      locale: $locale
      fallbackLocales: $fallbackLocale
      skip: $skip
    ) {
      _publishedAt
      slug
      id
      title
      seoTags {
        description
        image {
          responsiveImage {
            srcSet
            webpSrcSet
            sizes
            src
            width
            height
            aspectRatio
            alt
            title
            bgColor
            base64
          }
        }
      }
      speaker {
        name
        title
        bio
        slug
        picture {
          responsiveImage(imgixParams: { w: "64", h: "64", fit: crop }) {
            srcSet
            webpSrcSet
            sizes
            src
            width
            height
            aspectRatio
            alt
            title
            bgColor
            base64
          }
        }
      }
    }
    _allReferencingTalksMeta(
      locale: $locale
      filter: { title: { isBlank: "false" } }
    ) {
      count
    }
  }
}
